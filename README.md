# martin's emacs config

Just clone it.

```
$ git clone --recursive git://github.com/nanont/secam.git ~/.emacs.d
```

Things might not work on first start. In this case, run
```
M-x package-install
> use-package
```

Restart.

